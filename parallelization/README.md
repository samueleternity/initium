Note that both Chunked and DEER parallelizations failed. 

Chunked caused the model to get dumber because DNC is not compatible with it meanwhile DEER completely fried the performance of the training.
The codebase of that experiment is preserved in case it can be reused, fixed or optimized in the future.

The experiment will move in another direction and the workable model is in the "model" folder
